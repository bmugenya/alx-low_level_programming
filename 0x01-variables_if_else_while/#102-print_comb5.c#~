#include <stdio.h>

/** 
 * main - prints all possible different combinations of two two-digits number
 *
 * Return: Always 0
 */

int main()
{
  int x,y;

    for (x = 0; x < 98; x++)
    {
      for (y = x + 1; y < 99; y++)
        {
	 
	    putchar(x / 10 + '0');
            putchar(x % 10 + '0');
	    putchar(' ');
            putchar(j / 10 + '0');
	    putchar(x % 10 + '0');
          
	    if (x == 98 &&  y == 99)
	      break;
	    
	    putchar(',');
	    putchar(' ');
	    
        }
    }
    putchar('\n');
    return (0);
}
